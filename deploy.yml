- hosts: kubemaster
  become: yes
  become_user: kubeadmin
  tasks:
    - name: Copy {{ deploy }} deployment file
      copy:
        src: deployments/{{ deploy }}.yml
        dest: /tmp/

    - name: Deploy {{ deploy }}
      shell: kubectl create -f /tmp/{{ deploy }}.yml